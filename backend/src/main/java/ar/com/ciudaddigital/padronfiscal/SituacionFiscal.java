package ar.com.ciudaddigital.padronfiscal;

/** Resultado posible de consultar un CUIT contra el padrón fiscal (ADR 0020 §1). */
public enum SituacionFiscal {
    ACTIVO,
    INHABILITADO,
    NO_ENCONTRADO
}
